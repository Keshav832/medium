import { AuthLayout } from "../components/AuthLayout";
import { SignupAuth } from "../components/SignupAuth"

export const Signup = () => {
    return <AuthLayout>
        <SignupAuth />
    </AuthLayout>
}