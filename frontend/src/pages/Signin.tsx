import { AuthLayout } from "../components/AuthLayout";
import { SigninAuth } from "../components/SigninAuth"

export const Signin = () => {
    return <AuthLayout>
        <SigninAuth />
    </AuthLayout>
}